<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>practice</title>
</head>
<body>
    <link rel="stylesheet" href="styleop.css">
    <div class="container">
        <div class="to-do-list">
            <h1>
                to do list
                <img id="to-do-image" src="https://th.bing.com/th/id/OIP.0qE-iDevj1VjBvLxW6qfmQAAAA?rs=1&pid=ImgDetMain">
            </h1>
            <div class="row">
                <input type="text" id="input_id" placeholder="ENTER THE TASK">
                <button onclick="addTask()">ADD</button>
            </div>
            <ul id="list-container">
                <!-- <li id="checked">Task1</li>
                <li>Task2</li>
                <li>Task3</li> -->
                
                
            </ul>
        </div>
    </div>
    <script src="jsbasicsfile.js"></script>
</body>
</html>



<style>
.container {
    width: 100%;
    min-height: 100vh;
    background: linear-gradient(135deg, red, blue);
    padding: 10px;
}

.to-do-list {
    width: 100%;
    max-width: 540px;
    background-color: white;
    margin: 100px auto 20px;
    padding: 40px 30px 70px;
    border-radius: 10px;

}

#to-do-image {
    margin-left: 10px;
    width: 30px;
}

.to-do-list h1 {
    color: aqua;
    display: flex;
    align-items: center;
    margin-bottom: 20px;
}

.row {
    display: flex;
    justify-content: center;
    align-items: center;
    background: greenyellow;
    border-radius: 10px;
    padding-left: 2px;
    margin-bottom: 50px;
    padding: 5px;
}

#input_id {
    flex: 1;
    border: none;
    outline: none;
    background: transparent;
    padding: 10px;
}

button {
    border: none;
    outline: none;
    padding: 16px 40px;
    border-radius: 100px;
    background: yellow;
    cursor: pointer;
    font-size: 20px;
}

#list-container li {
    list-style: none;
    padding: 12px 8px 12px 50px;
    user-select: none;
    cursor: pointer;
    position:relative;
}
#list-container li::before{
    content:' ';
    position:absolute;
    height:28px;
    width:28px;
    border-radius:50%;
    background-position: center;
    background-size: cover;
    top:12px;
    left:8px;
    background-image: url(https://i.etsystatic.com/27170323/r/il/8b42ac/4169283903/il_fullxfull.4169283903_q3yk.jpg);
}
 ul  #checked{
    color:red;
    text-decoration: line-through;
 }

 ul #checked::before{
    background-image: url(https://static.vecteezy.com/system/resources/previews/010/152/436/original/tick-check-mark-icon-sign-symbol-design-free-png.png);
    height:30px;
    width:30px; 
}
ul li span{
    position:absolute;
    right:0;
    top: 5px;
    width:40px;
    height:40px;
    font-size:22px;
    color:red;
    line-height: 40px;
    text-align: center;
    border-radius:50%;
}
ul li span:hover{
    background:violet;
}
</style>

<srcipt>
const inputbox = document.getElementById("input_id");
const listContainer = document.getElementById("list-container");

function addTask() {
    if (inputbox.value === '') {
        alert("You have to enter something");
    } else {
        let li = document.createElement("li");
        li.innerHTML = inputbox.value;
        listContainer.appendChild(li);

        // Add delete button
        let span = document.createElement("span");
        span.innerHTML = "\u00d7"; // Unicode for '×' symbol
        li.appendChild(span);
    }
    inputbox.value = ''; // Clear input box
    saveData();
}

// Fixing the typo in addEventListener
listContainer.addEventListener("click", function (e) {
    if (e.target.tagName === "LI") {
        e.target.classList.toggle("checked"); // Toggle 'checked' class
        saveData();
    } else if (e.target.tagName === "SPAN") {
        e.target.parentElement.remove(); // Remove parent <li> element
        saveData();
    }
}, false);

// Fixing the typo in localStorage reference
function saveData() {
    localStorage.setItem("data", listContainer.innerHTML);
}

// Adding validation for localStorage data
function showTask() {
    const savedData = localStorage.getItem("data");
    if (savedData) {
        listContainer.innerHTML = savedData;
    }
}

// Call showTask on page load
showTask();
</script>
